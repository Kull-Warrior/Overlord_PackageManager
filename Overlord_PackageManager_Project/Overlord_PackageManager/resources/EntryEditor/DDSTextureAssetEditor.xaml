<UserControl x:Class="Overlord_PackageManager.resources.EntryEditor.DDSTextureAssetEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <!-- Der Inhalt (RootPanel) nimmt den restlichen Platz ein -->
            <RowDefinition Height="*" />
            <!-- Die Buttons erhalten eine feste oder automatische Höhe am unteren Rand -->
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Inhaltsbereich oben -->
        <ScrollViewer Grid.Row="0" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel x:Name="RootPanel"
                        Margin="10"
                        Orientation="Vertical"/>
        </ScrollViewer>

        <!-- Button-Bereich unten -->
        <Grid Grid.Row="1" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Content="Export DDS"
                    Grid.Column="0"
                    Margin="0,0,5,0"
                    Padding="5"
                    Click="Export_Click"/>

            <Button Content="Import DDS"
                    Grid.Column="1"
                    Margin="5,0,0,0"
                    Padding="5"
                    Click="Import_Click"/>
        </Grid>
    </Grid>
</UserControl>