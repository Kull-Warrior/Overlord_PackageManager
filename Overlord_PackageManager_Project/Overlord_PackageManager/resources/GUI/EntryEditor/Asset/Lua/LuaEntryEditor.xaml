<UserControl x:Class="Overlord_PackageManager.resources.GUI.EntryEditor.Asset.Lua.LuaEntryEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">

            <StackPanel x:Name="RootPanel"
                    Margin="10"
                    Orientation="Vertical"/>
        </ScrollViewer>

        <Grid Grid.Row="1" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button Content="Export Lua"
                Grid.Column="0"
                Margin="0,0,5,0"
                Click="Export_Click"/>

            <Button Content="Import Lua"
                Grid.Column="1"
                Margin="5,0,0,0"
                Click="Import_Click"/>

        </Grid>
    </Grid>
</UserControl>