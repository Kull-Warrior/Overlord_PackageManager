<UserControl x:Class="Overlord_PackageManager.resources.GUI.EntryEditor.Leaf.Scalar.BoneRotationEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Overlord_PackageManager.resources.GUI.EntryEditor.Leaf.Scalar">
    <UserControl.Resources>
        <Style x:Key="Vector4CellBorder" TargetType="Border">
            <Setter Property="Margin" Value="2" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#444" />
            <Setter Property="CornerRadius" Value="4" />
        </Style>
    </UserControl.Resources>

    <StackPanel Margin="5">
        <Label x:Name="MainLabel" FontWeight="Bold" Content="Bone Rotation" />

        <Grid Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Row 0 -->
            <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource Vector4CellBorder}">
                <local:FloatEditor x:Name="YawEditor" Label="Yaw" />
            </Border>
            <Border Grid.Row="0" Grid.Column="2" Style="{StaticResource Vector4CellBorder}">
                <local:FloatEditor x:Name="PitchEditor" Label="Pitch" />
            </Border>
            <Border Grid.Row="0" Grid.Column="3" Style="{StaticResource Vector4CellBorder}">
                <local:FloatEditor x:Name="RollEditor" Label="Roll" />
            </Border>
        </Grid>
    </StackPanel>
</UserControl>