<UserControl x:Class="Overlord_PackageManager.resources.GUI.EntryEditor.Leaf.Scalar.BonePositionEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Overlord_PackageManager.resources.GUI.EntryEditor.Leaf.Scalar">
    <UserControl.Resources>
        <Style x:Key="Vector4CellBorder" TargetType="Border">
            <Setter Property="Margin" Value="2" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#444" />
            <Setter Property="CornerRadius" Value="4" />
        </Style>
    </UserControl.Resources>

    <StackPanel Margin="5">
        <Label x:Name="MainLabel" FontWeight="Bold" Content="Bone Position" />

        <Grid Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Row 0 -->
            <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource Vector4CellBorder}">
                <local:FloatEditor x:Name="TimestampEditor" Label="Timestamp in µs" />
            </Border>
            <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource Vector4CellBorder}">
                <local:FloatEditor x:Name="XEditor" Label="X" />
            </Border>
            <Border Grid.Row="0" Grid.Column="2" Style="{StaticResource Vector4CellBorder}">
                <local:FloatEditor x:Name="YEditor" Label="Y" />
            </Border>
            <Border Grid.Row="0" Grid.Column="3" Style="{StaticResource Vector4CellBorder}">
                <local:FloatEditor x:Name="ZEditor" Label="Z" />
            </Border>
        </Grid>
    </StackPanel>
</UserControl>