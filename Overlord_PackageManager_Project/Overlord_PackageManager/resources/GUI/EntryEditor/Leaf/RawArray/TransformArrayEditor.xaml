<UserControl x:Class="Overlord_PackageManager.resources.GUI.EntryEditor.Leaf.RawArray.TransformArrayEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Overlord_PackageManager.resources.GUI.EntryEditor.Leaf.Scalar">

    <UserControl.Resources>
        <Style x:Key="ArrayCellBorder" TargetType="Border">
            <Setter Property="Margin" Value="2" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#444" />
            <Setter Property="CornerRadius" Value="4" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,10,10,5">
            <TextBlock Text="Transform Array" 
                       FontWeight="Bold" 
                       FontSize="16" 
                       VerticalAlignment="Center" />
            <Button Content="+ Add" 
                    Width="60" 
                    Margin="20,0,5,0" 
                    Click="AddButton_Click" />
            <Button Content="- Remove Last" 
                    Width="100" 
                    Margin="5,0,0,0" 
                    Click="RemoveButton_Click" />
            <TextBlock x:Name="CountBlock" 
                       Margin="20,0,0,0" 
                       Foreground="#888" 
                       VerticalAlignment="Center" 
                       FontSize="12" />
        </StackPanel>

        <!-- Array items -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ArrayItemsControl" Margin="10,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ArrayCellBorder}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <ContentPresenter Grid.Column="0" 
                                                  Content="{Binding Editor}" />

                                <Button Grid.Column="1" 
                                        Content="✕" 
                                        Width="24" 
                                        Height="24" 
                                        Background="#CC3333" 
                                        Foreground="White" 
                                        BorderThickness="0" 
                                        Click="RemoveSpecificButton_Click" 
                                        Tag="{Binding}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>